import React from 'react';

const Multiplepropsprac = (props) => {
    return (
        <div>
            <h1>{props.name}</h1>
            <p>{props.age}</p>
            <p>{props.city}</p>
        </div>
    );
}

export default Multiplepropsprac;
